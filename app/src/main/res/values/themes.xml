<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyProfile" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">#E91E63</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ButtonGeneral" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">@dimen/button_corner_radius</item>
        <item name="android:layout_width">0dp</item>
    </style>

    <style name="ButtonPrimaryColor" parent="ButtonGeneral">
        <item name="android:textColor">@color/white</item>
        <item name="android:layout_height">70dp</item>
        <item name="android:textSize">@dimen/primary_btn_text_size</item>
        <item name="android:layout_marginTop">30dp</item>
    </style>

    <style name="ButtonOutlined" parent="ButtonGeneral">
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textColor">@color/dark_blue</item>
        <item name="strokeColor">@color/dark_blue</item>
        <item name="android:layout_height">55dp</item>
        <item name="strokeWidth">2dp</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Medium</item>
    </style>

</resources>